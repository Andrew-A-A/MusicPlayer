package com.andrew.saba.musicplayer.viewmodel

import androidx.lifecycle.ViewModel

class AudioTrackViewModel:ViewModel() {
    // Manages the currently playing track, its details, and playback controls
    // Handles user interactions related to track playback
}